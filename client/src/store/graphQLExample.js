// import gql from "graphql-tag";

export default {
  namespace: true,
  state: {},
  getters: {},
  mutations: {},
  actions: {
    // async fetchCountries({ commit }) {
    //   const response = await gql.query({});
    // }
  }
};
